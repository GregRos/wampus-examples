import { WampType } from "../protocol/message.type";
/**
 * A set of prefix key routes used to identify common routes messages.
 * @see [WAMP Basic Profile]{@link https://wamp-proto.org/_static/wamp_latest.html#message-definitions}
 * @see [WAMP Advanced Profile]{@link https://wamp-proto.org/_static/wamp_latest.html#message-definitions-0}
 */
export declare module Routes {
    const goodbye: WampType[];
    const abort: WampType[];
    function error(type: WampType, param2?: number): number[];
    function published(publishReqId: number): number[];
    function subscribed(subReqId: number): number[];
    function unsubscribed(unsubReqId: number): number[];
    function event(subId: number): number[];
    function registered(registerReqId: number): number[];
    function unregistered(unregisterReqId: number): number[];
    function invocation(registrationId: number): number[];
    function result(reqId: number): number[];
}

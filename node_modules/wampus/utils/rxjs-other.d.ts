import { Observable } from "rxjs";
/**@internal*/
export interface EventEmitted {
    name: string;
    arg: any;
}
/**@internal*/
export declare class RxjsEventAdapter<T> {
    private _source;
    private _selector;
    private _events;
    private _sub;
    private _emitter;
    constructor(_source: Observable<T>, _selector: (x: T) => EventEmitted, _events: string[]);
    private _initEmitter;
    on(name: string, handler: (x: any) => void): void;
    off(name: string, handler: any): void;
}

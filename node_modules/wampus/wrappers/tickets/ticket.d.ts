export declare abstract class Ticket {
    abstract close(): Promise<void>;
    static combine(tickets: Ticket[]): any;
    add(...others: Ticket[]): any;
    using(actions: () => Promise<void>): Promise<void>;
}
export declare class CompoundTicket extends Ticket {
    _inner: Ticket[];
    constructor(_inner: Ticket[]);
    close(): Promise<void>;
}
